
# Cart Challenge

# Dependencies
- node
- yarn

# Setup
- `brew install node`
- `npm install -g yarn`
- (in project dir) `yarn`


# Start the app locally
- `yarn run serve`
- visit http://localhost:8080

